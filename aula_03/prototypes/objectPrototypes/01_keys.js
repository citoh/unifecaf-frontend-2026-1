/**
 * Object.keys
 *
 * O que faz:
 * Retorna um array com as chaves (enumeráveis) do objeto.
 *
 * Exemplo:
 * listar chaves
 *
 * Impressão do resultado (saída esperada):
 * [ 'id', 'nome' ]
 */

const user = { id: 1, nome: 'Ana' };
console.log(Object.keys(user));
