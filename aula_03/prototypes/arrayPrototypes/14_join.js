/**
 * Array.prototype.join
 *
 * O que faz:
 * Junta todos os elementos do array em uma string, usando um separador.
 *
 * Exemplo:
 * gerar CSV simples
 *
 * Impressão do resultado (saída esperada):
 * Ana,Bruno,Carla
 */

const nomes = ['Ana', 'Bruno', 'Carla'];
console.log(nomes.join(','));
