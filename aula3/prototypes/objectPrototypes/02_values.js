/**
 * Object.values
 *
 * O que faz:
 * Retorna um array com os valores (enumeráveis) do objeto.
 *
 * Exemplo:
 * listar valores
 *
 * Impressão do resultado (saída esperada):
 * [ 1, 'Ana' ]
 */

const user = { id: 1, nome: 'Ana' };
console.log(Object.values(user));
